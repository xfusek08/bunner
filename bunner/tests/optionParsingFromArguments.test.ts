import { describe, test } from "bun:test";

describe("Test Option Parsing of a command according to given definition", () => {
    test.todo("should return an error if an unknown option is provided");
});
